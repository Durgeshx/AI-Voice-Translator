# Core (required for hosted preview + demo mode)
streamlit>=1.32.0
plotly>=5.20.0
pandas>=2.0.0
numpy>=1.26.0
reportlab>=4.0.0
deep-translator>=1.11.0
python-dotenv>=1.0.0
emergentintegrations>=0.2.0

# For full live pipeline (microphone + Whisper + pyannote diarization),
# also install `requirements-local.txt`  (needs ~2GB disk + HF_TOKEN)
